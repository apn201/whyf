# runtime (goes into the Lambda bundle)
strands-agents>=0.1.0
boto3>=1.35.0
pyyaml>=6.0

# build and test only. keep out of the Lambda layer.
pytest>=8.0
openpyxl>=3.1        # reads the Annex I workbook
aws-cdk-lib>=2.150.0
constructs>=10.3.0
